import { createAction } from '@ngrx/store';

export const countIncrement = createAction('[Count Component] Count Increment');
export const countReduction = createAction('[Count Component] Count Reduction');
export const countReset = createAction('[Count Component] Count Reset');
